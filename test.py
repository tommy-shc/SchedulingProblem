import simpy

class car(self,env):
    def __init__(self,env):

        self.env = env
        self.action = env.process()

    def run(s)
    

env = simpy.Environment()
env.process(car(env))
env.run(until=15)